namespace ER.Infrastructure.Seeds.Data;

/// <summary>
/// Shared credentials used when seeding sample identity users.
/// </summary>
public static class SampleUserCredentials
{
    /// <summary>
    /// Default password assigned to all seeded sample users.
    /// </summary>
    public const string DefaultPassword = "SeedPass1!";
}
